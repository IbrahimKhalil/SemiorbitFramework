<?php 
use Semiorbit\field\Field;

$myField = Field::Create( $this->field );

?>
<?php if ( $this->Open ) : ?>

<div class="semiorbit-form-field <?= $myField->ControlDefaultCssClass() ?>" id="field-<?= $myField->Name ?>">
	
<?php endif;

if ( $this->Close ) : ?>

</div>

<?php endif;